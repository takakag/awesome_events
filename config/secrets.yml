# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

default_twitter: &default_twitter
  twitter_api_key: <%= ENV["TWITTER_CONSUMER_KEY"] %>
  twitter_api_secret: <% ENV["TWITTER_CONSUMER_SECRET"] %>

development:
  secret_key_base: 3f5be1bbadea386329727ada25bfe8253e2a3c3451b3afba062360abf568b1a9370a846ff4a00d54ef28f44f161fcf84c8d367030c35da9bbb3ca10a2adc4404
  <<: *default_twitter

staging:
  secret_key_base: 3f5be1bbadea386329727ada25bfe8253e2a3c3451b3afba062360abf568b1a9370a846ff4a00d54ef28f44f161fcf84c8d367030c35da9bbb3ca10a2adc4404
  <<: *default_twitter

test:
  secret_key_base: 54cdc5edd3057ea5b02faa116a81acd592715ff1e0e738d380562c0663287184b053b17226a45475ef98b10d045b6f89654db1f19a09b5765ff4f186af80b43a
  <<: *default_twitter

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  twitter_api_key: <%= ENV["TWITTER_CONSUMER_KEY"] %>
  twitter_api_secret: <%= ENV["TWITTER_CONSUMER_SECRET"] %>
